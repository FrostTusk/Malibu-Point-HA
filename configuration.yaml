# Configure a default setup of Home Assistant (frontend, api, etc)
default_config:

http:
  use_x_forwarded_for: true
  trusted_proxies: 127.0.0.1

# ---
# Entities
# ---
sensor: !include_dir_merge_list includes/entities/sensor
alarm_control_panel: !include_dir_merge_list includes/entities/alarm_control_panel
camera: !include_dir_merge_list includes/entities/camera
media_player: !include_dir_merge_list includes/entities/media_player

input_boolean: !include_dir_merge_named includes/entities/input_boolean
input_select: !include_dir_merge_named includes/entities/input_select
counter:  !include_dir_merge_named includes/entities/counter

glances: !include includes/entities/glances.yaml
tuya: !include includes/entities/tuya.yaml

# ---
# Services
# ---
rest_command: !include_dir_merge_named includes/services/rest_command

# ---
# Automations
# ---
script: !include includes/scripts.yaml
scene: !include includes/scenes.yaml
automation: !include_dir_merge_list includes/automations

# ---
# Interfaces
# ---
frontend:
  themes: !include_dir_named includes/themes/

climate:
  - platform: generic_thermostat
    name: Glances Google Test
    heater: input_boolean.googleglue
    target_sensor: sensor.ursus_core_0_temp

google_assistant:
  project_id: home-assistant-140f3
  service_account: !include SERVICE-ACCOUNT.json
  report_state: true
  exposed_domains:
    - switch
  entity_config:
    alarm_control_panel.smaug:
      room: 'Loft'
      expose: true
    media_player.taricha:
      room: 'Loft'
      expose: true
    input_select.taricha_ui:
      room: 'Loft'
      expose: true
    camera.amnirana_camera: # Doesn't work currently
      room: 'Loft'
      expose: true
    # sensor.ursus_core_0_temp:
    #   room: "Grandma's House"
    #   expose: true
    # sensor.ursus_core_1_temp:
    #   room: "Grandma's House"
    #   expose: true
    # sensor.ursus_battery_temp:
    #   room: "Grandma's House"
    #   expose: true
    # sensor.panthera_core_0_temp:
    #   room: 'Loft'
    # sensor.panthera_core_1_temp:
    #   room: 'Loft'
    # sensor.panthera_core_2_temp:
    #   room: 'Loft'
    # sensor.panthera_core_3_temp:
    #   room: 'Loft'
    # sensor.panthera_battery_temp:
    #   room: 'Loft'
    # sensor.hyaena_core_0_temp:
    #   room: "Grandma's House"
    # sensor.hyaena_core_1_temp:
    #   room: "Grandma's House"
    # sensor.ailurus_cpu_thermal_1_temp:
    #   room: 'Loft'
  secure_devices_pin: !secret google_secure_devices_pin

# ---
# Other
# ---
group: !include includes/groups.yaml

# notify:
#   - name: testFile
#     platform: file
#     filename: testFile

tts:
  - platform: google_translate
